// theme.js
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "'Clash Display', sans-serif", // For headings
    body: "'Mulish', sans-serif;" // For body text
  }
});

export default theme;
